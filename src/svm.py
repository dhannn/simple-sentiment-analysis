import pandas as pd
from sklearn.svm import SVC
'''
    Takes a features of training set (X) and labels of training set (y)
    and returns the multinomial SVC
'''
def naive_bayes(X_train: pd.DataFrame, y_train: pd.DataFrame) -> SVC:
    pass
